<div>
	<h2><%= @show.name %></h2>
	<%= @show.image.url(:medium) %>
	<% @show.episodes.each do |e| %>
		<div>
			<div>
				<%= e.title %>
			</div>
			<%= video_tag e.video.url, controls: true%>
		</div>
	<% end %>
</div>